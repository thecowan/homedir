
Host cotterpin groobee minga denali loo dgy tara tilly too mokey gorg prickle prickle.* astrobot astrobot.* gorg gorg.* home.cowlan.net syd.cowlan.net *.cowlan.net

Host astrobot astrobot.* gorg gorg.* home.cowlan.net syd.cowlan.net
   User root

Host *.cowlan.net
   ControlMaster auto

Host cotterpin cotterpin.* gorg gorg.* mokey mokey@
   ProxyCommand bash -c "grep ' 10.41.' <(ifconfig) > /dev/null && nc %h %p || ssh -W %h:%p admin@home.cowlan.net"
   ProxyUseFdpass no

Host groobee groobee.* prickle prickle.* minga minga.* denali denali.* rodgy rodgy.* tara tara.* tilly tilly.* too too.*
   #ProxyCommand bash -c "nc %h %p || ssh -W %h:%p root@home.cowlan.net"
   #ProxyCommand bash -c "nc %h %p || ssh -W %h:%p root@home.cowlan.net"
   ProxyCommand bash -c "grep ' 10.41.' <(ifconfig) > /dev/null && nc %h %p || ssh -W %h:%p admin@home.cowlan.net"
   ProxyUseFdpass no
   #UseProxyIf true
   #ProxyCommand bash -c "nc %h %p || ssh -W %h:%p root@home.cowlan.net"
   #ProxyCommand bash -c "nc %h %p || ssh -W %h:%p root@home.cowlan.net"

Host mokey
   HostName 10.2.17.169 

Host poe-switch
    HostKeyAlgorithms +ssh-rsa
    PubkeyAcceptedKeyTypes +ssh-rsa

Host *
   ControlMaster autoask
   ControlPath ~/.ssh/control/%r@%h:%p

ServerAliveInterval 60
ForwardAgent yes
User paul
